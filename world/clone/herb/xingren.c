// xingren.c

#include "herb.h"

void create()
{
        set_name(HIY "–”» " NOR, ({ "xingren", "herb_xingren" }));
        if (clonep())
                set_default_object(__FILE__);
        else {
                set("long", "’‚ «÷–“©“©≤ƒ°£\n");
                set("unit", "ø≈");
                set("value", 60);
                set("weight", 20);
set("base_unit","ø≈");
        }
        setup();
}
