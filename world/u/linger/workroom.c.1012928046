// This is a room made by roommaker.

inherit ROOM;

#include <ansi.h>
void create()
{
        set("short", RED"浴缸"NOR);
	set("long", "这是一间什么也没有的空房间。\n");
	setup();
	replace_program(ROOM);
}
